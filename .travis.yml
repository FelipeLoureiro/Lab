php:
  - 7.3

addons:
  apt:
    packages:
      - chromium-chromedriver

before_script:
  - ln -s /usr/lib/chromium-browser/chromedriver ~/bin/chromedriver
  - composer install
  - composer update
script:
  - php artisan dusk
